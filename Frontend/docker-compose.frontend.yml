version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scase-food-frontend
    restart: always
    ports:
      - "3000:80"  # Expose Nginx on port 3000

export enum Role {
    GHOST = 'GHOST',
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum Status {
    IDLE = 'IDLE',
    ONGOING = 'ONGOING'
}

export enum StatusOfOrder {
    UNPAIED = 'UNPAIED',
    PAIED = 'PAIED',
    DONE = 'DONE'
}